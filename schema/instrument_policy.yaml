version: 1
schema_id: instrument_metadata_policy
applies_to:
  template: templates/microscope_template.yaml
  instruments_glob: instruments/*.yaml
purpose:
  - drive instrument metadata completeness audits
  - define required vs conditional vs optional fields
  - provide one source of truth for validation and method-generator inclusion
  - reference controlled vocabularies without duplicating their contents

status_definitions:
  required: must be present for all instruments
  conditional: must be present when the stated condition is true
  optional: useful but not required for instrument-level reporting completeness

vocab_registry:
  modalities:
    source: file
    path: vocab/modalities.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  modules:
    source: file
    path: vocab/modules.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  scanner_types:
    source: file
    path: vocab/scanner_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  light_source_kinds:
    source: file
    path: vocab/light_source_kinds.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  detector_kinds:
    source: file
    path: vocab/detector_kinds.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  software_roles:
    source: file
    path: vocab/software_roles.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  stage_types:
    source: file
    path: vocab/stage_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  autofocus_types:
    source: file
    path: vocab/autofocus_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  triggering_modes:
    source: file
    path: vocab/triggering_modes.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  objective_immersion:
    source: file
    path: vocab/objective_immersion.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  objective_corrections:
    source: file
    path: vocab/objective_corrections.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  maintenance_action:
    source: file
    path: vocab/maintenance_action.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  maintenance_reason:
    source: file
    path: vocab/maintenance_reason.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  maintenance_status:
    source: file
    path: vocab/maintenance_status.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  metric_unit:
    source: file
    path: vocab/metric_unit.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_artifact_roles:
    source: file
    path: vocab/qc_artifact_roles.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_evaluation_status:
    source: file
    path: vocab/qc_evaluation_status.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_measurement_positions:
    source: file
    path: vocab/qc_measurement_positions.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_metric_classes:
    source: file
    path: vocab/qc_metric_classes.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_setpoint_units:
    source: file
    path: vocab/qc_setpoint_units.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  qc_type:
    source: file
    path: vocab/qc_type.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  service_provider:
    source: file
    path: vocab/service_provider.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  ocular_availability:
    source: file
    path: vocab/ocular_availability.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  mechanism_types:
    source: file
    path: vocab/mechanism_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  optical_component_types:
    source: file
    path: vocab/optical_component_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  stand_orientations:
    source: file
    path: vocab/stand_orientations.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  light_sheet_types:
    source: file
    path: vocab/light_sheet_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  optical_routes:
    source: file
    path: vocab/optical_routes.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  light_source_technologies:
    source: file
    path: vocab/light_source_technologies.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  light_source_roles:
    source: file
    path: vocab/light_source_roles.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  light_source_timing_modes:
    source: file
    path: vocab/light_source_timing_modes.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  splitter_types:
    source: file
    path: vocab/splitter_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  objective_specialties:
    source: file
    path: vocab/objective_specialties.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  optical_modulator_types:
    source: file
    path: vocab/optical_modulator_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  phase_mask_types:
    source: file
    path: vocab/phase_mask_types.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true
  adaptive_illumination_methods:
    source: file
    path: vocab/adaptive_illumination_methods.yaml
    canonical_ids_only: true
    allow_synonyms_on_input: true

sections:
  - id: general_identity
    title: General instrument identity
    rules:
      - path: instrument.display_name
        title: Display name
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Primary human-readable instrument name shown in reports and methods output.

      - path: instrument.instrument_id
        title: Instrument ID
        status: required
        type: slug
        validation:
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        used_by: [audit_pdf, method_generator, dashboard, qc_mapping, maintenance]
        rationale: Stable machine-readable key used across QC, maintenance, and dashboard routing.

      - path: instrument.manufacturer
        title: Manufacturer
        status: required
        type: string
        vocab: null
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for instrument identification and methods wording.

      - path: instrument.model
        title: Model
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for instrument identification and methods wording.

      - path: instrument.year_of_purchase
        title: Year of purchase
        status: optional
        type: year_or_empty
        used_by: [audit_pdf, dashboard]
        rationale: Operational context and lifecycle planning; not essential for methods completeness.

      - path: instrument.funding
        title: Funding
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Operational and acknowledgement context; not required for methods reporting.

      - path: instrument.stand_orientation
        title: Stand orientation
        status: required
        type: string
        vocab: stand_orientations
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Used directly by methods generator and helps classify the platform.

      - path: instrument.ocular_availability
        title: Ocular availability
        status: required
        type: string
        vocab: ocular_availability
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Critical user-experience metadata. Distinguishes traditional stands from box microscopes.

      - path: instrument.location
        title: Location
        status: required
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: required for operational metadata; not necessary for methods text.

      - path: instrument.notes
        title: Notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Free text is useful for staff but should not replace structured fields.

      - path: instrument.url
        title: Instrument URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Helpful for staff lookup and manuals, not essential for methods completeness.

      - path: modalities
        title: Imaging modalities
        status: required
        type: list
        item_type: string
        min_items: 1
        vocab: modalities
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core reporting category in QUAREP/JCB-style instrumentation metadata.

      - path: modules
        title: Installed modules
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Only present when installed modules materially change imaging capabilities.

      - path: modules[].name
        title: Module name
        status: conditional
        type: string
        vocab: modules
        required_if:
          parent_present: "modules[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required when a module/path is installed and materially changes imaging capabilities.

      - path: modules[].notes
        title: Module notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for operational nuances and limitations.

      - path: modules[].url
        title: Module URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Helpful vendor or manual reference.

      - path: modules[].manufacturer
        title: Module manufacturer
        status: conditional
        type: string
        required_if:
          parent_present: "modules[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required by 4DN-BINA/QUAREP for exact hardware provenance when a module is present.

      - path: modules[].model
        title: Module model
        status: conditional
        type: string
        required_if:
          parent_present: "modules[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required by 4DN-BINA/QUAREP for exact hardware provenance when a module is present.

  - id: software
    title: Software
    rules:
      - path: software[]
        title: Software entries
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Software is represented as extensible role-tagged entries.

      - path: software[].role
        title: Software role
        status: conditional
        type: string
        vocab: software_roles
        required_if:
          parent_present: "software[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Role tags define how software entries are interpreted in methods text and audits.

      - path: software[].name
        title: Software name
        status: conditional
        type: string
        required_if:
          parent_present: "software[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required for each declared software entry.

      - path: software[].version
        title: Software version
        status: conditional
        type: string_or_empty
        required_if:
          parent_present: "software[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Version is required (empty allowed) for reproducibility-aware software metadata.

      - path: software[].developer
        title: Software developer
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Helpful provenance metadata for software vendors/developers.

      - path: software[].notes
        title: Software notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for workflow caveats and role-specific implementation notes.

      - path: software[].url
        title: Software URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference link to vendor pages or manuals.

  - id: scanner
    title: Scanner
    rules:
      - path: hardware.scanner.type
        title: Scanner type
        status: required
        type: string
        vocab: scanner_types
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core hardware discriminator for methods completeness.

      - path: hardware.scanner.line_rate_hz
        title: Scanner line rate (Hz)
        status: conditional
        type: positive_number
        required_if:
          scanner_type_in: [resonant, polygon, acousto_optic, tandem]
        used_by: [audit_pdf, method_generator]
        rationale: Required where speed mode is intrinsic and relevant to the scan engine.

      - path: hardware.scanner.pinhole_um
        title: Pinhole diameter (um)
        status: conditional
        type: positive_number
        required_if:
          scanner_type_in: [spinning_disk]
        used_by: [audit_pdf, method_generator]
        rationale: Required for spinning disk systems where pinhole size is fixed in hardware. Point scanners use tunable pinholes defined per-experiment.

      - path: hardware.scanner.light_sheet_type
        title: Light Sheet Architecture
        status: conditional
        type: string
        vocab: light_sheet_types
        required_if:
          modalities_any_of: [light_sheet]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Clarifies the specific illumination geometry for light sheet systems.

      - path: hardware.scanner.notes
        title: Scanner notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for mode-specific caveats.

      - path: hardware.scanner.url
        title: Scanner URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

  - id: light_sources
    title: Light sources
    rules:
      - path: hardware.light_sources
        title: Light sources
        status: required
        type: list
        item_type: object
        min_items: 1
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Each instrument needs at least one declared excitation source.

      - path: hardware.light_sources[].kind
        title: Light source kind
        status: required
        type: string
        vocab: light_source_kinds
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core excitation-source class.

      - path: hardware.light_sources[].manufacturer
        title: Light source manufacturer
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for unambiguous hardware identification.

      - path: hardware.light_sources[].model
        title: Light source model
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for unambiguous hardware identification.

      - path: hardware.light_sources[].technology
        title: Light source technology subtype
        status: optional
        type: string
        vocab: light_source_technologies
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Helpful subtype information for lasers or LED engines.

      - path: hardware.light_sources[].path
        title: Light source path tag
        status: optional
        type: string
        vocab: optical_routes
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Optional path tag to associate a source with specific illumination routes.

      - path: hardware.light_sources[].role
        title: Light source role
        status: conditional
        type: string
        vocab: light_source_roles
        required_if:
          any_of:
            - item_field_in:
                kind: [laser]
            - item_field_in:
                type: [laser]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Structured role assignment separates excitation and STED depletion lasers without relying on free-text notes.

      - path: hardware.light_sources[].timing_mode
        title: Light source timing mode
        status: optional
        type: string
        vocab: light_source_timing_modes
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Declares whether the source is operated in continuous-wave or pulsed timing mode.

      - path: hardware.light_sources[].wavelength_nm
        title: Peak Wavelength (nm)
        status: conditional
        type: spectral_descriptor
        required_if:
          item_kind_in: [laser, led]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Peak wavelength value for discrete or center-defined excitation sources.

      - path: hardware.light_sources[].width_nm
        title: Bandwidth / FWHM (nm)
        status: optional
        type: positive_number
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Optional spectral width for LEDs and broad-spectrum excitation sources.

      - path: hardware.light_sources[].power
        title: Light source nominal power
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Nominal source power is useful hardware metadata, but actual acquisition power is experiment-level.

      - path: hardware.light_sources[].pulse_width_ps
        title: Light source pulse width (ps)
        status: conditional
        type: positive_number
        required_if:
          item_field_in:
            timing_mode: [pulsed]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Pulse width is required for pulsed sources to capture STED-relevant timing characteristics.

      - path: hardware.light_sources[].repetition_rate_mhz
        title: Light source repetition rate (MHz)
        status: conditional
        type: positive_number
        required_if:
          item_field_in:
            timing_mode: [pulsed]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Repetition rate is required for pulsed sources to support timing-aware method reporting.

      - path: hardware.light_sources[].depletion_targets_nm
        title: Depletion target wavelengths (nm)
        status: conditional
        type: list
        item_type: positive_number
        required_if:
          item_field_in:
            role: [depletion]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Structured STED depletion target wavelengths for depletion-designated sources.

      - path: hardware.light_sources[].notes
        title: Light source notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for software aliases and caveats.

      - path: hardware.light_sources[].url
        title: Light source URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

  - id: detectors
    title: Detectors
    rules:
      - path: hardware.detectors
        title: Detectors
        status: optional
        type: list
        item_type: object
        min_items: 1
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Optional to support purely manual microscopes; include at least one detector chain when digital detection is present.

      - path: hardware.detectors[].kind
        title: Detector kind
        status: conditional
        type: string
        vocab: detector_kinds
        required_if:
          parent_present: "hardware.detectors[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core detector class.

      - path: hardware.detectors[].manufacturer
        title: Detector manufacturer
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.detectors[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for hardware identification.

      - path: hardware.detectors[].model
        title: Detector model
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.detectors[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for hardware identification.

      - path: hardware.detectors[].pixel_pitch_um
        title: Detector pixel pitch (um)
        status: conditional
        type: positive_number
        aliases: ["hardware.detectors[].pixel_size_um"]
        required_if:
          item_kind_in: [scmos, cmos, emccd, ccd]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required for camera-based detectors because reporting completeness depends on it.

      - path: hardware.detectors[].pixel_size_um
        title: Detector pixel size (legacy alias)
        status: optional
        type: positive_number
        superseded_by: hardware.detectors[].pixel_pitch_um
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Backward-compatible alias retained for existing files; normalize to pixel_pitch_um when possible.

      - path: hardware.detectors[].sensor_format_px
        title: Detector sensor format (px)
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Useful capability metadata, not minimal.

      - path: hardware.detectors[].binning
        title: Detector binning
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Usually acquisition-specific rather than fixed hardware metadata.

      - path: hardware.detectors[].bit_depth
        title: Detector bit depth
        status: optional
        type: positive_number
        used_by: [audit_pdf, dashboard]
        rationale: Capability metadata; useful but not minimal.

      - path: hardware.detectors[].qe_peak_pct
        title: Detector peak quantum efficiency (%)
        status: conditional
        type: positive_number
        required_if:
          modalities_any_of: [confocal_point, confocal_spinning_disk, ism]
        used_by: [audit_pdf, dashboard]
        rationale: Capability metadata.

      - path: hardware.detectors[].read_noise_e
        title: Detector read noise (e-)
        status: optional
        type: positive_number
        used_by: [audit_pdf, dashboard]
        rationale: Capability metadata.

      - path: hardware.detectors[].notes
        title: Detector notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for channel assignments or operating modes.

      - path: hardware.detectors[].supports_time_gating
        title: Detector supports time gating
        status: optional
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Instrument-level capability flag indicating whether detector time-gating is available.

      - path: hardware.detectors[].default_gating_delay_ns
        title: Detector default gating delay (ns)
        status: conditional
        type: positive_number
        required_if:
          item_field_in:
            supports_time_gating: [true]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Facility default detector gating delay setting used for instrument characterization, not experiment truth.

      - path: hardware.detectors[].default_gate_width_ns
        title: Detector default gate width (ns)
        status: conditional
        type: positive_number
        required_if:
          item_field_in:
            supports_time_gating: [true]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Facility default detector gate width setting used for instrument characterization, not experiment truth.

      - path: hardware.detectors[].url
        title: Detector URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

  - id: optical_modulators
    title: Optical modulators
    rules:
      - path: hardware.optical_modulators
        title: Optical modulators
        status: optional
        type: list
        item_type: object
        min_items: 1
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Optional inventory of phase and beam-shaping devices used by STED or adaptive illumination workflows.

      - path: hardware.optical_modulators[].type
        title: Optical modulator type
        status: conditional
        type: string
        vocab: optical_modulator_types
        required_if:
          parent_present: "hardware.optical_modulators[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Structured classification of installed optical modulator hardware.

      - path: hardware.optical_modulators[].manufacturer
        title: Optical modulator manufacturer
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.optical_modulators[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required for reproducible hardware provenance when an optical modulator is listed.

      - path: hardware.optical_modulators[].model
        title: Optical modulator model
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.optical_modulators[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required for reproducible hardware provenance when an optical modulator is listed.

      - path: hardware.optical_modulators[].supported_phase_masks
        title: Supported phase masks
        status: optional
        type: list
        vocab: phase_mask_types
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Declares supported phase mask classes for depletion beam shaping.

      - path: hardware.optical_modulators[].notes
        title: Optical modulator notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Additional implementation notes for modulator integration.

  - id: illumination_logic
    title: Illumination logic
    rules:
      - path: hardware.illumination_logic
        title: Illumination logic entries
        status: optional
        type: list
        item_type: object
        min_items: 1
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Optional list of structured adaptive or conditional illumination logic supported by the instrument.

      - path: hardware.illumination_logic[].method
        title: Illumination logic method
        status: conditional
        type: string
        vocab: adaptive_illumination_methods
        required_if:
          parent_present: "hardware.illumination_logic[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Structured adaptive illumination method identifier.

      - path: hardware.illumination_logic[].default_enabled
        title: Illumination logic default enabled
        status: conditional
        type: boolean
        required_if:
          parent_present: "hardware.illumination_logic[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Facility default state for each illumination logic entry.

      - path: hardware.illumination_logic[].notes
        title: Illumination logic notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Additional context for adaptive illumination behavior and constraints.

  - id: filters_and_splitters
    title: Filters and splitters (deprecated)
    rules:
      - path: hardware.filters
        title: Installed filters or filter sets
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].name
        title: Filter or filter-set name
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].location
        title: Filter location
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].product_code
        title: Filter product code
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Helpful for staff verification.

      - path: hardware.filters[].excitation
        title: Filter excitation band
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].dichroic
        title: Filter dichroic
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].emission
        title: Filter emission band
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.filters[].notes
        title: Filter notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful for operational nuance.

      - path: hardware.filters[].url
        title: Filter URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

      - path: hardware.splitters
        title: Installed splitters
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.splitters[].name
        title: Splitter name
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.splitters[].type
        title: Splitter type
        status: optional
        type: string
        vocab: splitter_types
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Deprecated flat path retained for backward compatibility; prefer hardware.light_path graph model.

      - path: hardware.splitters[].notes
        title: Splitter notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful operational nuance.

      - path: hardware.splitters[].url
        title: Splitter URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

  - id: light_path_graph
    title: Light path topology graph (mechanisms and optical components)
    rules:
      - path: hardware.light_path
        title: Light path topology
        status: optional
        type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Topological graph model for modern virtual microscope UI and Tier 3 reporting.

      - path: hardware.light_path.cube_mechanisms
        title: Cube mechanisms
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]

      - path: hardware.light_path.cube_mechanisms[].name
        title: Cube mechanism name
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[]"

      - path: hardware.light_path.cube_mechanisms[].type
        title: Cube mechanism type
        status: conditional
        type: string
        vocab: mechanism_types
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[]"

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.bands
        title: Cube excitation multiband array
        status: conditional
        type: list
        item_type: object

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.bands[].center_nm
        title: Excitation band center (nm)
        status: conditional
        type: positive_number
        
      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.bands[].width_nm
        title: Excitation band width (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.bands
        title: Cube emission multiband array
        status: conditional
        type: list
        item_type: object

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.bands[].center_nm
        title: Emission band center (nm)
        status: conditional
        type: positive_number
        
      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.bands[].width_nm
        title: Emission band width (nm)
        status: conditional
        type: positive_number
        
      - path: hardware.light_path.cube_mechanisms[].path
        title: Cube mechanism path tag
        status: optional
        type: string
        vocab: optical_routes

      - path: hardware.light_path.cube_mechanisms[].notes
        title: Cube mechanism notes
        status: optional
        type: text

      - path: hardware.light_path.cube_mechanisms[].slots
        title: Cube mechanism slots
        status: conditional
        type: positive_number
        required_if:
          all_of:
            - parent_present: "hardware.light_path.cube_mechanisms[]"
            - field_equals_any:
                field: "hardware.light_path.cube_mechanisms[].type"
                values: [filter_wheel, slider, turret]

      - path: hardware.light_path.cube_mechanisms[].positions
        title: Cube mechanism positions map
        status: optional
        type: object

      - path: hardware.light_path.cube_mechanisms[].positions{}.path
        title: Cube position path tag
        status: optional
        type: string
        vocab: optical_routes

      - path: hardware.light_path.cube_mechanisms[].positions{}.name
        title: Cube name
        status: optional
        type: string

      - path: hardware.light_path.cube_mechanisms[].positions{}.product_code
        title: Cube product code
        status: optional
        type: string

      - path: hardware.light_path.cube_mechanisms[].positions{}.notes
        title: Cube notes
        status: optional
        type: text

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter
        title: Cube position excitation filter
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}"

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.component_type
        title: Cube excitation filter component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}.excitation_filter"

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.center_nm
        title: Cube excitation center (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.cube_mechanisms[].positions{}.excitation_filter.width_nm
        title: Cube excitation width (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.cube_mechanisms[].positions{}.dichroic.cutoffs_nm
        title: Cube dichroic cutoffs (nm)
        status: conditional
        type: list
        item_type: positive_number

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.center_nm
        title: Cube emission center (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.width_nm
        title: Cube emission width (nm)
        status: conditional
        type: positive_number
        
      - path: hardware.light_path.cube_mechanisms[].positions{}.dichroic
        title: Cube position dichroic
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}"

      - path: hardware.light_path.cube_mechanisms[].positions{}.dichroic.component_type
        title: Cube dichroic component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}.dichroic"

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter
        title: Cube position emission filter
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}"

      - path: hardware.light_path.cube_mechanisms[].positions{}.emission_filter.component_type
        title: Cube emission filter component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.cube_mechanisms[].positions{}.emission_filter"

      - path: hardware.light_path.excitation_mechanisms
        title: Excitation mechanisms
        status: optional
        type: list
        item_type: object

      - path: hardware.light_path.excitation_mechanisms[].name
        title: Excitation mechanism name
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.light_path.excitation_mechanisms[]"

      - path: hardware.light_path.excitation_mechanisms[].type
        title: Excitation mechanism type
        status: conditional
        type: string
        vocab: mechanism_types
        required_if:
          parent_present: "hardware.light_path.excitation_mechanisms[]"

      - path: hardware.light_path.excitation_mechanisms[].path
        title: Excitation mechanism path tag
        status: optional
        type: string
        vocab: optical_routes

      - path: hardware.light_path.excitation_mechanisms[].notes
        title: Excitation mechanism notes
        status: optional
        type: text

      - path: hardware.light_path.excitation_mechanisms[].slots
        title: Excitation mechanism slots
        status: conditional
        type: positive_number
        required_if:
          all_of:
            - parent_present: "hardware.light_path.excitation_mechanisms[]"
            - field_equals_any:
                field: "hardware.light_path.excitation_mechanisms[].type"
                values: [filter_wheel, slider, turret]

      - path: hardware.light_path.excitation_mechanisms[].positions
        title: Excitation mechanism positions map
        status: optional
        type: object

      - path: hardware.light_path.excitation_mechanisms[].positions{}.component_type
        title: Excitation position component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.excitation_mechanisms[].positions{}"

      - path: hardware.light_path.excitation_mechanisms[].positions{}.manufacturer
        title: Excitation position component manufacturer
        status: optional
        type: string

      - path: hardware.light_path.excitation_mechanisms[].positions{}.product_code
        title: Excitation position component product code
        status: optional
        type: string

      - path: hardware.light_path.excitation_mechanisms[].positions{}.center_nm
        title: Excitation position center wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.excitation_mechanisms[].positions{}.width_nm
        title: Excitation position bandwidth (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.excitation_mechanisms[].positions{}.cut_on_nm
        title: Excitation position cut-on wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.excitation_mechanisms[].positions{}.cut_off_nm
        title: Excitation position cut-off wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.excitation_mechanisms[].positions{}.cutoffs_nm
        title: Excitation position multi-cutoff wavelengths (nm)
        status: conditional
        type: list
        item_type: positive_number

      - path: hardware.light_path.dichroic_mechanisms
        title: Dichroic mechanisms
        status: optional
        type: list
        item_type: object

      - path: hardware.light_path.dichroic_mechanisms[].name
        title: Dichroic mechanism name
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.light_path.dichroic_mechanisms[]"

      - path: hardware.light_path.dichroic_mechanisms[].type
        title: Dichroic mechanism type
        status: conditional
        type: string
        vocab: mechanism_types
        required_if:
          parent_present: "hardware.light_path.dichroic_mechanisms[]"

      - path: hardware.light_path.dichroic_mechanisms[].path
        title: Dichroic mechanism path tag
        status: optional
        type: string
        vocab: optical_routes

      - path: hardware.light_path.dichroic_mechanisms[].notes
        title: Dichroic mechanism notes
        status: optional
        type: text

      - path: hardware.light_path.dichroic_mechanisms[].slots
        title: Dichroic mechanism slots
        status: conditional
        type: positive_number
        required_if:
          all_of:
            - parent_present: "hardware.light_path.dichroic_mechanisms[]"
            - field_equals_any:
                field: "hardware.light_path.dichroic_mechanisms[].type"
                values: [filter_wheel, slider, turret]

      - path: hardware.light_path.dichroic_mechanisms[].positions
        title: Dichroic mechanism positions map
        status: optional
        type: object

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.component_type
        title: Dichroic position component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.dichroic_mechanisms[].positions{}"

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.manufacturer
        title: Dichroic position component manufacturer
        status: optional
        type: string

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.product_code
        title: Dichroic position component product code
        status: optional
        type: string

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.center_nm
        title: Dichroic position center wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.width_nm
        title: Dichroic position bandwidth (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.cut_on_nm
        title: Dichroic position cut-on wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.cut_off_nm
        title: Dichroic position cut-off wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.dichroic_mechanisms[].positions{}.cutoffs_nm
        title: Dichroic position multi-cutoff wavelengths (nm)
        status: conditional
        type: list
        item_type: positive_number

      - path: hardware.light_path.emission_mechanisms
        title: Emission mechanisms
        status: optional
        type: list
        item_type: object
      
      - path: hardware.light_path.emission_mechanisms[].name
        title: Emission mechanism name
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.light_path.emission_mechanisms[]"

      - path: hardware.light_path.emission_mechanisms[].type
        title: Emission mechanism type
        status: conditional
        type: string
        vocab: mechanism_types
        required_if:
          parent_present: "hardware.light_path.emission_mechanisms[]"

      - path: hardware.light_path.emission_mechanisms[].path
        title: Emission mechanism path tag
        status: optional
        type: string
        vocab: optical_routes

      - path: hardware.light_path.emission_mechanisms[].notes
        title: Emission mechanism notes
        status: optional
        type: text

      - path: hardware.light_path.emission_mechanisms[].slots
        title: Emission mechanism slots
        status: conditional
        type: positive_number
        required_if:
          all_of:
            - parent_present: "hardware.light_path.emission_mechanisms[]"
            - field_equals_any:
                field: "hardware.light_path.emission_mechanisms[].type"
                values: [filter_wheel, slider, turret]

      - path: hardware.light_path.emission_mechanisms[].positions
        title: Emission mechanism positions map
        status: optional
        type: object

      - path: hardware.light_path.emission_mechanisms[].positions{}.component_type
        title: Emission position component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.emission_mechanisms[].positions{}"

      - path: hardware.light_path.emission_mechanisms[].positions{}.manufacturer
        title: Emission position component manufacturer
        status: optional
        type: string

      - path: hardware.light_path.emission_mechanisms[].positions{}.product_code
        title: Emission position component product code
        status: optional
        type: string

      - path: hardware.light_path.emission_mechanisms[].positions{}.center_nm
        title: Emission position center wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.emission_mechanisms[].positions{}.width_nm
        title: Emission position bandwidth (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.emission_mechanisms[].positions{}.cut_on_nm
        title: Emission position cut-on wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.emission_mechanisms[].positions{}.cut_off_nm
        title: Emission position cut-off wavelength (nm)
        status: conditional
        type: positive_number

      - path: hardware.light_path.emission_mechanisms[].positions{}.cutoffs_nm
        title: Emission position multi-cutoff wavelengths (nm)
        status: conditional
        type: list
        item_type: positive_number

      - path: hardware.light_path.splitters
        title: Light path splitters
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]

      - path: hardware.light_path.splitters[].name
        title: Splitter name
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.light_path.splitters[]"

      - path: hardware.light_path.splitters[].type
        title: Splitter type
        status: conditional
        type: string
        vocab: splitter_types
        required_if:
          parent_present: "hardware.light_path.splitters[]"

      - path: hardware.light_path.splitters[].dichroic
        title: Splitter dichroic
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.splitters[]"

      - path: hardware.light_path.splitters[].dichroic.component_type
        title: Splitter dichroic component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.splitters[].dichroic"

      - path: hardware.light_path.splitters[].dichroic.cut_on_nm
        title: Splitter dichroic cutoff wavelength (nm)
        status: conditional
        type: positive_number
        required_if:
          parent_present: "hardware.light_path.splitters[].dichroic"

      - path: hardware.light_path.splitters[].path_1
        title: Splitter path 1 (transmitted)
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.splitters[]"

      - path: hardware.light_path.splitters[].path_1.emission_filter.component_type
        title: Splitter path 1 emission filter component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.splitters[].path_1.emission_filter"

      - path: hardware.light_path.splitters[].path_2
        title: Splitter path 2 (reflected)
        status: conditional
        type: object
        required_if:
          parent_present: "hardware.light_path.splitters[]"

      - path: hardware.light_path.splitters[].path_2.emission_filter.component_type
        title: Splitter path 2 emission filter component type
        status: conditional
        type: string
        vocab: optical_component_types
        required_if:
          parent_present: "hardware.light_path.splitters[].path_2.emission_filter"

  - id: objectives
    title: Objectives
    rules:
      - path: hardware.objectives
        title: Objectives
        status: required
        type: list
        item_type: object
        min_items: 1
        used_by: [audit_pdf, method_generator, dashboard, qc_mapping]
        rationale: Objective inventory is central to instrument completeness and QC mapping.

      - path: hardware.objectives[].id
        title: Objective ID
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard, qc_mapping]
        rationale: Stable per-objective key used for QC mapping.

      - path: hardware.objectives[].manufacturer
        title: Objective manufacturer
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for objective identification.

      - path: hardware.objectives[].model
        title: Objective model
        status: required
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Needed for objective identification.

      - path: hardware.objectives[].product_code
        title: Objective product code
        status: optional
        type: string
        used_by: [audit_pdf, dashboard]
        rationale: Very useful for staff lookup, but not minimal.

      - path: hardware.objectives[].magnification
        title: Objective magnification
        status: required
        type: positive_number
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core reporting field.

      - path: hardware.objectives[].numerical_aperture
        title: Objective numerical aperture
        status: required
        type: positive_number
        validation:
          min: 0
          max: 1.7
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core reporting field.

      - path: hardware.objectives[].working_distance
        title: Objective working distance
        status: conditional
        type: string
        required_if:
          modalities_any_of: [confocal_point, confocal_spinning_disk, ism]
        used_by: [audit_pdf, dashboard]
        rationale: Helpful reference metadata, not usually minimal for methods.

      - path: hardware.objectives[].immersion
        title: Objective immersion
        status: required
        type: string
        vocab: objective_immersion
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Core reporting field.

      - path: hardware.objectives[].correction
        title: Objective correction class
        status: required
        type: string
        vocab: objective_corrections
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Important to characterize the objective class.

      - path: hardware.objectives[].afc_compatible
        title: AFC compatible
        status: optional
        type: boolean
        used_by: [audit_pdf, dashboard]
        rationale: Operational capability metadata.

      - path: hardware.objectives[].is_installed
        title: Is objective installed
        status: required
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Prevents methods generator and dashboard from advertising unavailable optics.

      - path: hardware.objectives[].specialties
        title: Objective specialties
        status: optional
        type: list
        item_type: string
        vocab: objective_specialties
        used_by: [audit_pdf, dashboard]
        rationale: Useful capability annotations.

      - path: hardware.objectives[].notes
        title: Objective notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful free-text nuance.

      - path: hardware.objectives[].url
        title: Objective URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.

  - id: magnification_changers
    title: Magnification changers
    rules:
      - path: hardware.magnification_changers
        title: Magnification changers
        status: optional
        type: list
        item_type: object
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Present only when optovar or additional magnification changers are installed.

      - path: hardware.magnification_changers[].manufacturer
        title: Magnification changer manufacturer
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.magnification_changers[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required when such a device is installed.

      - path: hardware.magnification_changers[].model
        title: Magnification changer model
        status: conditional
        type: string
        required_if:
          parent_present: "hardware.magnification_changers[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required when such a device is installed.

      - path: hardware.magnification_changers[].magnification
        title: Magnification changer factor
        status: conditional
        type: positive_number
        required_if:
          parent_present: "hardware.magnification_changers[]"
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Required when such a device is installed.

      - path: hardware.magnification_changers[].notes
        title: Magnification changer notes
        status: optional
        type: text
        used_by: [audit_pdf, dashboard]
        rationale: Useful nuance.

      - path: hardware.magnification_changers[].url
        title: Magnification changer URL
        status: optional
        type: url
        used_by: [audit_pdf, dashboard]
        rationale: Reference only.


  - id: advanced_hardware
    title: Advanced hardware features
    rules:
      - path: hardware.environment.temperature_control
        title: Temperature control available
        status: conditional
        type: boolean
        required_if:
          any_of:
            - modules_any_of: [incubation]
            - modules_any_of: [environmental_enclosure]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether the system supports active temperature control for environmental regulation.

      - path: hardware.environment.temperature_range
        title: Temperature control range
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Documents the supported temperature range or setpoint range for live-cell or environmentally controlled imaging.

      - path: hardware.environment.co2_control
        title: CO2 control available
        status: optional
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether the system supports active CO2 regulation for live-cell imaging.

      - path: hardware.environment.co2_range
        title: CO2 control range
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Documents the supported CO2 range or configured operating range when gas control is available.

      - path: hardware.environment.o2_control
        title: O2 control available
        status: optional
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether the system supports active oxygen control for hypoxia or specialized environmental studies.

      - path: hardware.environment.o2_range
        title: O2 control range
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Documents the supported O2 range or configured operating range when oxygen control is available.

      - path: hardware.environment.humidity_control
        title: Humidity control available
        status: optional
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether the system provides humidity control as part of environmental regulation.

      - path: hardware.environment.notes
        title: Environmental control notes
        status: optional
        type: text
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Free-text notes for environmental chamber configuration, limitations, or operating guidance.

      - path: hardware.stages
        title: Stages and focus drives
        status: conditional
        type: list
        item_type: object
        required_if:
          any_of:
            - modules_any_of: [motorized_stage]
            - modules_any_of: [piezo_z_stage]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Present when motorized or specialized stage and focus hardware is documented.

      - path: hardware.stages[].type
        title: Stage or focus drive type
        status: conditional
        type: string
        vocab: stage_types
        required_if:
          any_of:
            - modules_any_of: [motorized_stage]
            - modules_any_of: [piezo_z_stage]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Classifies the stage or focus-drive hardware that is installed.

      - path: hardware.stages[].manufacturer
        title: Stage manufacturer
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Helps identify the installed stage or focus-drive hardware.

      - path: hardware.stages[].model
        title: Stage model
        status: optional
        type: string
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Helps identify the installed stage or focus-drive hardware.

      - path: hardware.stages[].step_size_um
        title: Stage step size (um)
        status: optional
        type: positive_number
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Useful capability metadata for motorized stage or focus-drive hardware.

      - path: hardware.hardware_autofocus.is_installed
        title: Hardware autofocus installed
        status: optional
        type: boolean
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether a hardware autofocus system is currently installed.

      - path: hardware.hardware_autofocus.type
        title: Hardware autofocus type
        status: conditional
        type: string
        vocab: autofocus_types
        required_if:
          modules_any_of: [hardware_autofocus]
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Describes the installed hardware autofocus implementation when present.

      - path: hardware.triggering.primary_mode
        title: Triggering primary mode
        status: optional
        type: string
        vocab: triggering_modes
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Indicates whether synchronization is primarily hardware-triggered, software-triggered, or mixed.

      - path: hardware.triggering.notes
        title: Triggering notes
        status: optional
        type: text
        used_by: [audit_pdf, method_generator, dashboard]
        rationale: Free-text notes for synchronization, timing, or external trigger details.

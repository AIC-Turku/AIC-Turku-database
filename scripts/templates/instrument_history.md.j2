# {{ instrument.display_name }} — History

[⬅ Back to specifications](spec.md){ .md-button }

<div class="aic-stack">

!!! info "Log summary"
    **QC sessions:** {{ qc_events | length }} · **Maintenance events:** {{ maintenance_events | length }}

</div>

=== "QC events"

    | Date | Overall | Reason | Operator | Link |
    |---|---|---|---|---|
    {% for e in qc_events %}
    | {{ e.date | default('—', true) }} | `{{ e.overall_status | default('—', true) }}` | {{ e.reason | default('—', true) }} | {{ e.operator | default('—', true) }} | [open](../../events/{{ e.event_id }}.md) |
    {% else %}
    | — | — | — | — | — |
    {% endfor %}

=== "Maintenance"

    | Date | Status after | Reason | Provider | Link |
    |---|---|---|---|---|
    {% for e in maintenance_events %}
    | {{ e.date | default('—', true) }} | `{{ e.status_after | default('—', true) }}` | {{ e.reason | default('—', true) }} | {{ e.provider | default('—', true) }} | [open](../../events/{{ e.event_id }}.md) |
    {% else %}
    | — | — | — | — | — |
    {% endfor %}

=== "Trends"

    <div
      class="aic-charts"
      data-aic-charts='{{ charts_data | tojson | safe }}'
      data-aic-metric-names='{{ metric_names | tojson | safe }}'
    ></div>

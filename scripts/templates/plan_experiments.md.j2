---
title: Experiment planning
hide:
  - navigation
  - toc
---

# Experiment planning

Good imaging starts with a clear measurement need and a realistic understanding of the available instrumentation. The references below are useful primers on quantitative fluorescence microscopy and experimental bias.

## Background reading

* [Accuracy and precision in quantitative fluorescence microscopy – A foundational guide to collecting quantitative data](https://rupress.org/jcb/article/185/7/1135/35453/Accuracy-and-precision-in-quantitative)
* [Designing a rigorous microscopy experiment – Validating methods and avoiding bias](https://rupress.org/jcb/article/218/5/1452/120908/Designing-a-rigorous-microscopy-experiment)
* [Believing is seeing – The deceptive influence of bias in quantitative microscopy](https://journals.biologists.com/jcs/article/137/1/jcs261567/341218/Believing-is-seeing-the-deceptive-influence-of)

---

## Compare microscopes with an assistant

You can use ChatGPT, Claude, Copilot, or another assistant to explore {{ facility_short_name }} microscopes using the facility inventory below. Attach the inventory to the conversation so facility-specific claims stay grounded in what {{ facility_short_name }} records.

!!! note "Planning aid"
    Use the assistant to identify recorded options and questions to follow up. Staff can confirm sample compatibility, access, training, availability, and configuration details that are not captured in the inventory.

### 1. Download the facility inventory

[Download the facility inventory (JSON)]({{ llm_inventory_asset_url }}){: download="llm_inventory.json" .md-button .md-button--primary style="margin-top: 10px; margin-bottom: 20px;" }

### 2. Describe what you need

Fill in the box below in your own words. Be as specific as useful, but you do not need to translate your experiment into microscope terminology first.

<div style="background-color: var(--md-default-bg-color); padding: 15px; border: 1px solid var(--md-default-fg-color--light); border-radius: 4px; margin-bottom: 15px;">
  <label for="experiment-desc" style="font-weight: bold; display: block; margin-bottom: 8px;">What do you want to do?</label>
  <textarea id="experiment-desc" rows="4" style="width: 100%; padding: 10px; border: 1px solid var(--md-default-fg-color--light); border-radius: 4px; background-color: var(--md-default-bg-color); font-family: inherit; resize: vertical;" placeholder="For example: I need TIRF imaging, or I need to compare the recorded options for a long two-colour time-lapse."></textarea>
</div>

### 3. Copy the prompt

Attach the inventory file you just downloaded to your assistant, then copy the prompt below. It tells the assistant to use the inventory as the source for facility-specific claims and to keep unrecorded details explicit.

<div style="position: relative; margin-bottom: 20px;">
  <button id="copy-prompt-btn" class="md-button md-button--primary" style="position: absolute; top: 10px; right: 10px; z-index: 10;">Copy prompt</button>
  <textarea id="generated-prompt" rows="18" style="width: 100%; padding: 15px; padding-top: 50px; border-radius: 4px; border: 1px solid var(--md-default-fg-color--light); background-color: var(--md-code-bg-color); font-family: monospace; line-height: 1.5; resize: vertical;" readonly></textarea>
</div>

<script id="plan-experiments-config" type="application/json">
{{ plan_experiments_config_json | safe }}
</script>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const configNode = document.getElementById("plan-experiments-config");
    let pageConfig = {
      facility_short_name: "Core Imaging Facility",
      facility_contact_url: "#",
      facility_contact_label: "Contact Core Imaging Facility Staff",
      llm_inventory_asset_url: "assets/llm_inventory.json",
    };
    try {
      if (configNode?.textContent?.trim()) {
        pageConfig = JSON.parse(configNode.textContent);
      }
    } catch (error) {
      console.error("Failed to parse plan-experiments config", error);
    }

    const facilityShortName = String(pageConfig?.facility_short_name || "Core Imaging Facility");
    const inputArea = document.getElementById("experiment-desc");
    const outputArea = document.getElementById("generated-prompt");
    const copyBtn = document.getElementById("copy-prompt-btn");

    const basePromptTop = `You are an experienced microscopy facility scientist helping me plan an experiment using the attached ${facilityShortName} inventory.

Use the JSON as the only source for facility-specific facts. Use general microscopy knowledge to interpret the biology, infer imaging requirements, explain trade-offs, and make evidence-weighted recommendations.

Core principle: reason aggressively about the experiment; be conservative about undocumented facility facts.

Rules:
- Read planning_contract first.
- A component missing from a complete route hardware list is not on that route; other missing or null values are unknown.
- Treat llm_context.authoritative_route_contract as route truth. Never combine components from different routes.
- Objectives are instrument-level unless explicit route compatibility is recorded.
- Preserve branches, splitters and endpoints. Read selection_mode before inferring simultaneous detection.
- Use capability_route_reconciliation and route_family_coverage to relate TIRF, STED, SIM and other capabilities to recorded route families.
- The inventory records no booking, access or training availability. Operational/QC status is not booking availability.
- You may infer experimental fit from recorded features and general microscopy principles, but never invent facility-specific performance, configuration or compatibility. If speed, depth, phototoxicity, sample compatibility or fluorophore compatibility is not established, treat it as unknown.
- Surface contradictory records rather than silently resolving them.
- Distinguish recorded fact, deduction, expert inference and unknown when it matters.

Procedure:
1. Translate the biological request into the imaging constraints that matter.
2. Use microscopy principles to identify plausible strategies and trade-offs.
3. Map them onto recorded routes and verify route membership, relevant detectors/endpoints and branch architecture.
4. Rank candidates by experimental fit. When justified, say which system you would investigate first and why.
5. Mention only one or two unknowns that could change the choice and what to confirm with ${facilityShortName} staff.
6. Ask one clarifying question only if it could materially change the strategy or shortlist.

My request:
`;

    const basePromptBottom = `

Return a concise, decision-oriented answer: recommendation or short ordered shortlist; why it fits; a useful alternative when relevant; and one or two important unknowns.

Do not dump every matching microscope or default to "ask staff" because some performance data are missing. Do not force a best microscope or backup when the evidence cannot support one. Do not invent facility specifications.`;

    function updatePrompt() {
      const userText = inputArea.value.trim();
      const desc = userText ? userText : "[Describe what you want to image or measure, in your own words.]";
      outputArea.value = basePromptTop + desc + basePromptBottom;
    }

    inputArea.addEventListener("input", updatePrompt);
    updatePrompt();

    copyBtn.addEventListener("click", () => {
      outputArea.select();
      outputArea.setSelectionRange(0, 99999);
      navigator.clipboard.writeText(outputArea.value).then(() => {
        const originalText = copyBtn.innerText;
        copyBtn.innerText = "Copied";
        setTimeout(() => { copyBtn.innerText = originalText; }, 2000);
      }).catch(err => {
        console.error('Failed to copy text: ', err);
      });
    });
  });
</script>

---

## Discuss the plan with {{ facility_short_name }} staff

The inventory and prompt can identify recorded options and gaps. {{ facility_short_name }} staff can check sample preparation, fluorophore choices, access, training, availability, and instrument-specific constraints before you book.

[{{ facility_contact_label }}]({{ facility_contact_url }}){: target="_blank" .md-button }

# Event: {{ event_id }}

<div class="aic-topactions" markdown="span">
  [🔙 Back to {{ instrument_id }} overview](../instruments/{{ instrument_id }}/){ .md-button }
  [📊 Back to history](../instruments/{{ instrument_id }}/history.md){ .md-button .md-button--primary }
</div>

- **Date:** {{ date | default('—', true) }}
- **Instrument:** {{ instrument | default(instrument_id, true) | default('—', true) }}
- **Operator:** {{ operator | default('—', true) }}

```yaml
{{ raw_yaml_content }}
```

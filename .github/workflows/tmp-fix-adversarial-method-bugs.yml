name: Fix adversarial method bugs

on:
  push:
    branches:
      - fix/methods-generator-pragmatic-stabilization

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/methods-generator-pragmatic-stabilization
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install -r requirements-test.txt
      - name: Apply fixes
        run: python scripts/_tmp_fix_adversarial_method_bugs_v2.py
      - name: Regenerate template
        run: python -m scripts.generate_templates
      - name: Targeted validation
        run: |
          python -m scripts.validate
          PYTHONPATH=. python -m pytest -q tests/test_methods_generator_method_first.py tests/test_active_yaml_contracts.py tests/test_validate_instrument_policy.py tests/test_facility_portability.py
          git diff --check
      - name: Commit fixes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Fix adversarial method-path bugs'
          git push origin HEAD:fix/methods-generator-pragmatic-stabilization

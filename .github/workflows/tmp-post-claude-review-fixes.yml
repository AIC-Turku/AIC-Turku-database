name: Apply post-Claude review fixes

on:
  push:
    branches:
      - fix/methods-generator-pragmatic-stabilization

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/methods-generator-pragmatic-stabilization
          fetch-depth: 0
      - name: Apply fixes
        run: python scripts/_tmp_fix_post_claude_review.py
      - name: Commit fixes and remove one-shot helpers
        run: |
          rm scripts/_tmp_fix_post_claude_review.py
          rm .github/workflows/tmp-post-claude-review-fixes.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix post-review Methods Generator regressions"
          git push origin HEAD:fix/methods-generator-pragmatic-stabilization

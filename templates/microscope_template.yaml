# AUTO-GENERATED FILE. Do not edit manually.
# Source schema: schema/instrument_policy.yaml

# -----------------------------------------------------------------------------
# INSTRUMENT
# -----------------------------------------------------------------------------
instrument:
  display_name: ""  # Display name: Primary human-readable instrument name shown in reports and methods output.
  instrument_id: ""  # Instrument ID: Stable machine-readable key used across QC, maintenance, and dashboard routing.
  manufacturer: ""  # Manufacturer: Company/vendor/brand that makes the instrument.
  model: ""  # Model: Vendor-facing model designation used as the primary structured identity field for the instrument; do not substitute display names or catalog codes.
  year_of_purchase: ""  # Year of purchase: Operational context and lifecycle planning; not essential for methods completeness.
  funding: ""  # Funding: Operational and acknowledgement context; not required for methods reporting.
  stand_orientation: ""  # Stand orientation: Used directly by methods generator and helps classify the platform. | Allowed values (canonical IDs): upright, inverted, stereo, benchtop, other
  ocular_availability: ""  # Ocular availability: Critical user-experience metadata. Distinguishes traditional stands from box microscopes. | Allowed values (canonical IDs): camera_only, binocular, trinocular, unknown
  location: ""  # Location: required for operational metadata; not necessary for methods text.
  notes: ""  # Notes: Free text is useful for staff but should not replace structured fields.
  url: ""  # Instrument URL: Helpful for staff lookup and manuals, not essential for methods completeness.

# -----------------------------------------------------------------------------
# IMAGING MODALITIES
# -----------------------------------------------------------------------------
modalities:  # Imaging modalities: Core reporting category in QUAREP/JCB-style instrumentation metadata. | Allowed values (canonical IDs): widefield_fluorescence, transmitted_brightfield, phase_contrast, polarized_light, dic, darkfield, confocal_point, confocal_spinning_disk, tirf, tirf_sim, light_sheet, multiphoton, shg, thg, optical_sectioning, sim, sted, resolft, smlm, spt, ism, flim, fcs, frap, photoactivation, fret, spectral_imaging, afm, impedance_cytometry, live_cell_imaging, reflected_brightfield, epi, confocal, transmitted
  - ""

# -----------------------------------------------------------------------------
# CAPABILITIES
# -----------------------------------------------------------------------------
capabilities:  # Capability axes: Canonical capability taxonomy that separates imaging modes, contrast methods, readouts, workflows, assay operations, and non-optical capabilities.
  imaging_modes:  # Capability imaging modes: Canonical imaging-mode capability axis (not a route identifier). | Allowed values (canonical IDs): confocal_point, confocal_spinning_disk, widefield_fluorescence, tirf, multiphoton, light_sheet, sim, sted, resolft, smlm, ism
    - ""
  contrast_methods:  # Capability contrast methods: Canonical contrast-method capability axis. | Allowed values (canonical IDs): transmitted_brightfield, reflected_brightfield, phase_contrast, dic, darkfield, polarized_light, optical_sectioning
    - ""
  readouts:  # Capability measurement readouts: Canonical measurement-readout capability axis. | Allowed values (canonical IDs): spectral_imaging, flim, fcs, fret
    - ""
  workflows:  # Capability workflow tags: Workflow/application tags; not optical route types. | Allowed values (canonical IDs): live_cell_imaging, high_content_screening, super_resolution, single_molecule, single_particle_tracking
    - ""
  assay_operations:  # Capability assay operations: Assay-operation capabilities such as FRAP/photoactivation; distinct from readout-only axes. | Allowed values (canonical IDs): frap, photoactivation, photobleaching, uncaging
    - ""
  non_optical:  # Capability non-optical: Non-optical capability tags; not optical light-path route types. | Allowed values (canonical IDs): afm, impedance_cytometry
    - ""

# -----------------------------------------------------------------------------
# HARDWARE MODULES
# -----------------------------------------------------------------------------
modules:  # Installed modules: Only present when installed modules materially change imaging capabilities.
  -
    type: ""  # Module type: Canonical module classifier used for structured capability reporting. | Allowed values (canonical IDs): incubation, microfluidics, hardware_autofocus, motorized_stage, frap, photoactivation, fcs, flim, optogenetics, adaptive_optics, piezo_z_stage, easy3d_sted, rescue_sted, 3d_sim, ring_tirf, tirf, aobs, environmental_enclosure, sim_module, afm_module, airyscan, hardware_triggering, workstation, impedance_module, universal_vessel_holder, camera_port
    name: ""  # Module display name (legacy): Legacy/local display label. Prefer modules[].type for canonical module identity.
    notes: ""  # Module notes: Useful for operational nuances and limitations.
    url: ""  # Module URL: Helpful vendor or manual reference.
    manufacturer: ""  # Module manufacturer: Vendor-facing module model/designation used as primary structured identity for the installed module.
    model: ""  # Module model: Vendor-facing module model/designation used as primary structured identity for the installed module.

# -----------------------------------------------------------------------------
# SOFTWARE STATUS
# -----------------------------------------------------------------------------
software_status: ""  # Software status: Declares whether software metadata is documented, unknown, or not applicable for manual/standalone instruments.

# -----------------------------------------------------------------------------
# SOFTWARE
# -----------------------------------------------------------------------------
software:  # Software entries: Software is represented as extensible role-tagged entries.
  -
    role: ""  # Software role: Role tags define how software entries are interpreted in methods text and audits. | Allowed values (canonical IDs): acquisition, processing, analysis, hardware_control, other
    name: ""  # Software name: Required for each declared software entry.
    version: ""  # Software version: Version is required (empty allowed) for reproducibility-aware software metadata.
    developer: ""  # Software developer: Helpful provenance metadata for software vendors/developers.
    notes: ""  # Software notes: Useful for workflow caveats and role-specific implementation notes.
    url: ""  # Software URL: Reference link to vendor pages or manuals.

# -----------------------------------------------------------------------------
# HARDWARE CONFIGURATION
# -----------------------------------------------------------------------------
hardware:
  scanner:
    type: ""  # Scanner type: Core hardware discriminator for methods completeness. | Allowed values (canonical IDs): none, galvo, resonant, tandem, spinning_disk, polygon, acousto_optic, stage_scanning
    line_rate_hz: ""  # Scanner line rate (Hz): Required where speed mode is intrinsic and relevant to the scan engine.
    pinhole_um: ""  # Pinhole diameter (um): Required for spinning disk systems where pinhole size is fixed in hardware. Point scanners use tunable pinholes defined per-experiment.
    light_sheet_type: ""  # Light Sheet Architecture: Clarifies the specific illumination geometry for light sheet systems. | Allowed values (canonical IDs): standard_spim, lattice, single_objective, di_spim, scape, airy_beam, other
    notes: ""  # Scanner notes: Useful for mode-specific caveats.
    url: ""  # Scanner URL: Reference only.
  detectors:  # Detectors: Optional to support purely manual microscopes; include at least one detector chain when digital detection is present.
    -
      id: ""  # Detector ID: Stable graph target ID for light path routing, virtual microscope traversal, and splitter branch targets.
      kind: ""  # Detector kind: Core detector class. | Allowed values (canonical IDs): pmt, gaasp_pmt, hyd, scmos, cmos, emccd, ccd, apd, spad
      manufacturer: ""  # Detector manufacturer: Needed for hardware identification.
      model: ""  # Detector model: Needed for hardware identification.
      pixel_pitch_um: ""  # Detector pixel pitch (um): Required for camera-based detectors because reporting completeness depends on it.
      pixel_size_um: ""  # Detector pixel size (legacy alias): Backward-compatible alias retained for existing files; normalize to pixel_pitch_um when possible.
      sensor_format_px: ""  # Detector sensor format (px): Useful capability metadata, not minimal.
      binning: ""  # Detector binning: Usually acquisition-specific rather than fixed hardware metadata.
      bit_depth: ""  # Detector bit depth: Capability metadata; useful but not minimal.
      qe_peak_pct: ""  # Detector peak quantum efficiency (%): Capability metadata.
      read_noise_e: ""  # Detector read noise (e-): Capability metadata.
      notes: ""  # Detector notes: Useful for channel assignments or operating modes.
      supports_time_gating: false  # Detector supports time gating: Instrument-level capability flag indicating whether detector time-gating is available.
      time_gating_defaults_known: false  # Detector time-gating defaults known: Indicates whether facility-authored default gating delay/width values are documented for this detector.
      default_gating_delay_ns: ""  # Detector default gating delay (ns): Facility default detector gating delay setting used for instrument characterization, not experiment truth.
      default_gate_width_ns: ""  # Detector default gate width (ns): Facility default detector gate width setting used for instrument characterization, not experiment truth.
      collection_min_nm: ""  # Detector collection window minimum wavelength (nm): Explicit lower bound of detector collection window for virtual microscope response modeling.
      collection_max_nm: ""  # Detector collection window maximum wavelength (nm): Explicit upper bound of detector collection window for virtual microscope response modeling.
      collection_center_nm: ""  # Detector collection window center wavelength (nm): Collection center wavelength used to derive detector passband when min/max are not supplied.
      collection_width_nm: ""  # Detector collection window width (nm): Collection bandwidth used with collection_center_nm to derive detector passband.
      channel_center_nm: ""  # Detector channel center wavelength (nm, legacy alias): Deprecated alias retained for compatibility; prefer collection_center_nm.
      bandwidth_nm: ""  # Detector bandwidth (nm, legacy alias): Deprecated alias retained for compatibility; prefer collection_width_nm.
      min_nm: ""  # Detector minimum wavelength (nm, legacy alias): Deprecated alias retained for compatibility; prefer collection_min_nm.
      max_nm: ""  # Detector maximum wavelength (nm, legacy alias): Deprecated alias retained for compatibility; prefer collection_max_nm.
      url: ""  # Detector URL: Reference only.
  optical_modulators:  # Optical modulators: Optional inventory of phase and beam-shaping devices used by STED or adaptive illumination workflows.
    -
      type: ""  # Optical modulator type: Structured classification of installed optical modulator hardware. | Allowed values (canonical IDs): slm, phase_plate, vortex_plate, deformable_mirror, aod, eom, other
      manufacturer: ""  # Optical modulator manufacturer: Required for reproducible hardware provenance when an optical modulator is listed.
      model: ""  # Optical modulator model: Required for reproducible hardware provenance when an optical modulator is listed.
      supported_phase_masks:  # Supported phase masks: Declares supported phase mask classes for depletion beam shaping. | Allowed values (canonical IDs): vortex, top_hat, bottle, z_sted, 3d_sted
        - ""
      notes: ""  # Optical modulator notes: Additional implementation notes for modulator integration.
  illumination_logic:  # Illumination logic entries: Optional list of structured adaptive or conditional illumination logic supported by the instrument.
    -
      method: ""  # Illumination logic method: Structured adaptive illumination method identifier. | Allowed values (canonical IDs): rescue_sted, confocal_sted_switching, roi_adaptive_illumination, patterned_depletion_control
      default_enabled: false  # Illumination logic default enabled: Facility default state for each illumination logic entry.
      notes: ""  # Illumination logic notes: Additional context for adaptive illumination behavior and constraints.
  objectives:  # Objectives: Objective inventory is central to instrument completeness and QC mapping.
    -
      id: ""  # Objective ID: Stable per-objective key used for QC mapping.
      manufacturer: ""  # Objective manufacturer: Needed for objective identification.
      model: ""  # Objective model: Needed for objective identification.
      product_code: ""  # Objective product code: Explicit catalog/order/SKU/reference code only; optional and must not be inferred from model or name.
      magnification: ""  # Objective magnification: Core reporting field.
      numerical_aperture: ""  # Objective numerical aperture: Core reporting field.
      working_distance: ""  # Objective working distance: Helpful reference metadata, not usually minimal for methods.
      immersion: ""  # Objective immersion: Core reporting field. | Allowed values (canonical IDs): air, oil, water, glycerol, silicone, multi, dipping
      correction: ""  # Objective correction class: Important to characterize the objective class. | Allowed values (canonical IDs): plan_apochromat, apochromat, plan_fluorite, fluorite, plan_achromat, achromat
      afc_compatible: false  # AFC compatible: Operational capability metadata.
      is_installed: false  # Is objective installed: Prevents methods generator and dashboard from advertising unavailable optics.
      specialties:  # Objective specialties: Useful capability annotations. | Allowed values (canonical IDs): long_working_distance, coverslip_correction, water_dipping, phase, dic, uv_transmission, ir_transmission, correction_collar, tirf, sim, sted, polarization, cleared_tissue_imaging, other
        - ""
      notes: ""  # Objective notes: Useful free-text nuance.
      url: ""  # Objective URL: Reference only.
  magnification_changers:  # Magnification changers: Present only when optovar or additional magnification changers are installed.
    -
      manufacturer: ""  # Magnification changer manufacturer: Required when such a device is installed.
      model: ""  # Magnification changer model: Required when such a device is installed.
      magnification: ""  # Magnification changer factor: Required when such a device is installed.
      notes: ""  # Magnification changer notes: Useful nuance.
      url: ""  # Magnification changer URL: Reference only.
  environment:
    temperature_control: false  # Temperature control available: Indicates whether the system supports active temperature control for environmental regulation.
    temperature_range: ""  # Temperature control range: Documents the supported temperature range or setpoint range for live-cell or environmentally controlled imaging.
    co2_control: false  # CO2 control available: Indicates whether the system supports active CO2 regulation for live-cell imaging.
    co2_range: ""  # CO2 control range: Documents the supported CO2 range or configured operating range when gas control is available.
    o2_control: false  # O2 control available: Indicates whether the system supports active oxygen control for hypoxia or specialized environmental studies.
    o2_range: ""  # O2 control range: Documents the supported O2 range or configured operating range when oxygen control is available.
    humidity_control: false  # Humidity control available: Indicates whether the system provides humidity control as part of environmental regulation.
    notes: ""  # Environmental control notes: Free-text notes for environmental chamber configuration, limitations, or operating guidance.
  stages:  # Stages and focus drives: Present when motorized or specialized stage and focus hardware is documented.
    -
      type: ""  # Stage or focus drive type: Classifies the stage or focus-drive hardware that is installed. | Allowed values (canonical IDs): xy_motorized, z_piezo, manual, other
      manufacturer: ""  # Stage manufacturer: Helps identify the installed stage or focus-drive hardware.
      model: ""  # Stage model: Helps identify the installed stage or focus-drive hardware.
      step_size_um: ""  # Stage step size (um): Useful capability metadata for motorized stage or focus-drive hardware.
  hardware_autofocus:
    is_installed: false  # Hardware autofocus installed: Indicates whether a hardware autofocus system is currently installed.
    type: ""  # Hardware autofocus type: Describes the installed hardware autofocus implementation when present. | Allowed values (canonical IDs): infrared_reflection, contrast_based, laser_reflection, capacitance_based, other
  triggering:
    primary_mode: ""  # Triggering primary mode: Indicates whether synchronization is primarily hardware-triggered, software-triggered, or mixed. | Allowed values (canonical IDs): hardware, software, mixed
    notes: ""  # Triggering notes: Free-text notes for synchronization, timing, or external trigger details.
  sources:  # Sources: Canonical illumination-source inventory referenced by ordered light_paths illumination_sequence entries.
    -
      id: ""  # Source ID: Stable source identifier referenced by light_paths illumination_sequence.
      name: ""  # Source display name: Optional display label for the installed source when it differs from the vendor model/designation.
      kind: ""  # Source kind: Canonical source classifier used for inventory reporting and light-path DTO generation. | Allowed values (canonical IDs): laser, white_light_laser, led, arc_lamp, metal_halide, halogen_lamp, multiphoton_laser, supercontinuum
      manufacturer: ""  # Source manufacturer: Structured provenance for installed sources.
      model: ""  # Source model: Vendor-facing source model/designation.
      product_code: ""  # Source product code: Explicit source SKU/catalog code when known.
      technology: ""  # Source technology subtype: Helpful subtype metadata for lasers, LED engines, and tunable sources. | Allowed values (canonical IDs): diode, dpss, led_array, supercontinuum, ti_sapphire, other
      role: ""  # Source role: Declares the source role without relying on notes-based inference. | Allowed values (canonical IDs): excitation, depletion, activation, alignment, transmitted_illumination, reflected_illumination
      timing_mode: ""  # Source timing mode: Declares whether the source is continuous-wave or pulsed. | Allowed values (canonical IDs): cw, pulsed
      wavelength_nm: ""  # Source wavelength (nm): Peak or representative wavelength for discrete excitation sources.
      width_nm: ""  # Source bandwidth / FWHM (nm): Optional spectral width for broad-spectrum or band-defined sources.
      power: ""  # Source nominal power: Nominal source power metadata; not experiment-level truth.
      pulse_width_ps: ""  # Source pulse width (ps): Required when pulsed timing is declared so timing-sensitive consumers receive explicit metadata.
      repetition_rate_mhz: ""  # Source repetition rate (MHz): Required when pulsed timing is declared so timing-sensitive consumers receive explicit metadata.
      depletion_targets_nm:  # Source depletion target wavelengths (nm): Required when the source is used as a depletion source.
        - ""
      tunable_min_nm: ""  # Source tunable minimum wavelength: Lower tunable wavelength bound for tunable or white-light sources.
      tunable_max_nm: ""  # Source tunable maximum wavelength: Upper tunable wavelength bound for tunable or white-light sources.
      simultaneous_lines_max: ""  # Source simultaneous line count: Maximum simultaneously active lines for multi-line sources.
      modalities:  # Source modalities: Legacy compatibility modality hints for installed sources. Ordered light_paths and route_type remain authoritative. | Allowed values (canonical IDs): widefield_fluorescence, transmitted_brightfield, phase_contrast, polarized_light, dic, darkfield, confocal_point, confocal_spinning_disk, tirf, tirf_sim, light_sheet, multiphoton, shg, thg, optical_sectioning, sim, sted, resolft, smlm, spt, ism, flim, fcs, frap, photoactivation, fret, spectral_imaging, afm, impedance_cytometry, live_cell_imaging, reflected_brightfield, epi, confocal, transmitted
        - ""
      notes: ""  # Source notes: Free-text source notes for operational caveats.
      url: ""  # Source URL: Reference link only.
  optical_path_elements:  # Optical path elements: Canonical optical mechanisms/components referenced by ordered light_paths traversal sequences.
    -
      id: ""  # Optical path element ID: Stable identifier referenced by light_paths sequences.
      name: ""  # Optical path element name: Human-readable element label for authoring clarity and consumer display.
      display_label: ""  # Optical path element display label: Optional alternate display label for UI-facing consumers.
      stage_role: ""  # Optical path element stage role: Descriptive grouping metadata for UI/runtime consumers. `light_paths` remain the sole canonical source of route topology, so stage_role should only be supplied when a stable stage grouping is genuinely useful. | Allowed values (canonical IDs): excitation, cube, dichroic, emission, analyzer, splitter
      element_type: ""  # Optical path element type: Canonical mechanism/selector/splitter type used for validator, DTO, and runtime handling. | Allowed values (canonical IDs): filter_wheel, slider, turret, fixed, tunable, spectral_slider, spectral_array, selector, splitter, emission_splitter, image_splitter, dual_view, quad_view
      selection_mode: ""  # Optical path element selection mode: Inventory-level selector/splitter capability metadata. Canonical route branching now lives in light_paths sequence branch blocks. | Allowed values (canonical IDs): fixed, exclusive, multiple
      supported_branch_modes:  # Supported branch modes: Inventory-level capability metadata describing which branch modes a selector/splitter can support. | Allowed values (canonical IDs): transmitted, reflected, selected, passthrough
        - ""
      supported_branch_count: ""  # Supported branch count: Inventory-level capability metadata describing how many branches a selector/splitter can support.
      modalities:  # Optical path element modalities: Legacy compatibility modality hints for optical path elements. Ordered light_paths and route_type remain authoritative. | Allowed values (canonical IDs): widefield_fluorescence, transmitted_brightfield, phase_contrast, polarized_light, dic, darkfield, confocal_point, confocal_spinning_disk, tirf, tirf_sim, light_sheet, multiphoton, shg, thg, optical_sectioning, sim, sted, resolft, smlm, spt, ism, flim, fcs, frap, photoactivation, fret, spectral_imaging, afm, impedance_cytometry, live_cell_imaging, reflected_brightfield, epi, confocal, transmitted
        - ""
      positions:  # Optical path element positions: Position map for installed filter wheels, turrets, sliders, or cubes.
        Pos_1:
          component_type: ""  # Optical path position component type: Canonical optical component classifier for direct position payloads. | Allowed values (canonical IDs): bandpass, multiband_bandpass, longpass, shortpass, dichroic, multiband_dichroic, polychroic, notch, filter_cube, analyzer, empty, mirror, block, passthrough, neutral_density
          manufacturer: ""  # Optical path position manufacturer: Structured provenance for direct position payloads.
          model: ""  # Optical path position model: Vendor-facing model/designation for direct position payloads.
          product_code: ""  # Optical path position product code: Explicit catalog/SKU/reference code for direct position payloads when known.
          bands:  # Optical path position multiband array: Explicit multiband pass windows for direct position payloads.
            -
              center_nm: ""  # Optical path position band center (nm): Band center for direct multiband position payloads.
              width_nm: ""  # Optical path position band width (nm): Band width for direct multiband position payloads.
          excitation_filter:  # Filter cube excitation filter: Explicit excitation-filter sub-component of a filter_cube position.
            component_type: ""  # Excitation filter component type: Canonical component classifier for the excitation filter sub-component. Inferred by the parser when omitted. | Allowed values (canonical IDs): bandpass, multiband_bandpass, longpass, shortpass, dichroic, multiband_dichroic, polychroic, notch, filter_cube, analyzer, empty, mirror, block, passthrough, neutral_density
            center_nm: ""  # Excitation filter center wavelength (nm): Band center for bandpass-type excitation filters.
            width_nm: ""  # Excitation filter bandwidth (nm): Band width for bandpass-type excitation filters.
            bands:  # Excitation filter multiband array: Explicit multiband pass windows for multiband excitation filters.
              -
                center_nm: ""  # Excitation filter band center (nm): Band center for multiband excitation filter sub-component.
                width_nm: ""  # Excitation filter band width (nm): Band width for multiband excitation filter sub-component.
          dichroic:  # Filter cube dichroic: Explicit dichroic sub-component of a filter_cube position.
            component_type: ""  # Dichroic component type: Canonical component classifier for the dichroic sub-component. Inferred by the parser when omitted. | Allowed values (canonical IDs): bandpass, multiband_bandpass, longpass, shortpass, dichroic, multiband_dichroic, polychroic, notch, filter_cube, analyzer, empty, mirror, block, passthrough, neutral_density
            cut_on_nm: ""  # Dichroic cut-on wavelength (nm): Single-edge dichroic cut-on wavelength.
            cutoffs_nm:  # Dichroic cutoff wavelengths (nm): Multi-edge dichroic cutoff wavelength array.
              - ""
          emission_filter:  # Filter cube emission filter: Explicit emission-filter sub-component of a filter_cube position.
            component_type: ""  # Emission filter component type: Canonical component classifier for the emission filter sub-component. Inferred by the parser when omitted. | Allowed values (canonical IDs): bandpass, multiband_bandpass, longpass, shortpass, dichroic, multiband_dichroic, polychroic, notch, filter_cube, analyzer, empty, mirror, block, passthrough, neutral_density
            center_nm: ""  # Emission filter center wavelength (nm): Band center for bandpass-type emission filters.
            width_nm: ""  # Emission filter bandwidth (nm): Band width for bandpass-type emission filters.
            cut_on_nm: ""  # Emission filter cut-on wavelength (nm): Cut-on wavelength for longpass-type emission filters.
            bands:  # Emission filter multiband array: Explicit multiband pass windows for multiband emission filters.
              -
                center_nm: ""  # Emission filter band center (nm): Band center for multiband emission filter sub-component.
                width_nm: ""  # Emission filter band width (nm): Band width for multiband emission filter sub-component.
  eyepieces:  # Eyepieces: Explicit terminal eyepieces addressed directly by light path routing.
    -
      id: ""  # Eyepiece ID: Stable identifier referenced by light_paths.
      magnification: ""  # Eyepiece magnification: Optional magnification value for the eyepiece.
      field_number: ""  # Eyepiece field number: Optional field number value for the eyepiece.
      notes: ""  # Eyepiece notes: Useful for documenting eyepiece-specific caveats or labels.
  endpoints:  # Explicit detection endpoints: Optional explicit endpoint inventory; canonical normalization merges these rows with endpoint-capable inventories into the unified endpoint registry.
    -
      id: ""  # Explicit endpoint ID: Stable identifier for the explicit endpoint row.
      endpoint_type: ""  # Explicit endpoint type: Canonical endpoint classifier for an explicit endpoint row. | Allowed values (canonical IDs): detector, camera_port, eyepiece

# -----------------------------------------------------------------------------
# LIGHT PATHS
# -----------------------------------------------------------------------------
light_paths:  # Light paths: Canonical ordered YAML-first traversal definitions. These sequences are the authoritative topology contract.
  -
    id: ""  # Light path ID: Stable route identifier used by simulator, UI, and graph rendering.
    name: ""  # Light path name: Human-readable route label shown in UI and exported DTO route catalogs.
    route_type: ""  # Light path route type: Controlled optical route type. light_paths[].id remains a stable local identifier and may be descriptive. | Allowed values (canonical IDs): epi, widefield_fluorescence, tirf, confocal, confocal_point, confocal_spinning_disk, multiphoton, light_sheet, transmitted, transmitted_brightfield, phase_contrast, darkfield, dic, reflected_brightfield, optical_sectioning, sim, sted, resolft, ism, smlm
    readouts:  # Light path readouts: Optional per-route measurement/readout tags associated with a route traversal. | Allowed values (canonical IDs): spectral_imaging, flim, fcs, fret
      - ""
    modalities:  # Light path modalities: Canonical modality IDs explicitly mapped to this route. When light_paths are declared, every populated route must define a non-empty modalities list so route-aware consumers never have to infer modality coverage. | Allowed values (canonical IDs): widefield_fluorescence, transmitted_brightfield, phase_contrast, polarized_light, dic, darkfield, confocal_point, confocal_spinning_disk, tirf, tirf_sim, light_sheet, multiphoton, shg, thg, optical_sectioning, sim, sted, resolft, smlm, spt, ism, flim, fcs, frap, photoactivation, fret, spectral_imaging, afm, impedance_cytometry, live_cell_imaging, reflected_brightfield, epi, confocal, transmitted
      - ""
    illumination_sequence:  # Illumination sequence: Ordered sequence of strict tagged-union items used before the sample plane; each item must declare exactly one of source_id or optical_path_element_id.
      -
        source_id: ""  # Illumination source reference: Stable source reference for the source_id variant of an illumination sequence tagged union item.
        optical_path_element_id: ""  # Illumination optical path element reference: Stable optical-path-element reference for the optical_path_element_id variant of an illumination sequence tagged union item.
    detection_sequence:  # Detection sequence: Ordered sequence of strict tagged-union items used after the sample plane; each item must declare exactly one of optical_path_element_id, endpoint_id, or branches.
      -
        optical_path_element_id: ""  # Detection optical path element reference: Stable optical-path-element reference for the optical_path_element_id variant of a detection sequence tagged union item.
        endpoint_id: ""  # Detection endpoint reference: Stable endpoint reference for the endpoint_id variant of a detection sequence tagged union item.
        branches:  # Detection branch block: Explicit route-owned one-level branch block for the branches variant of a detection sequence tagged union item.
          selection_mode: ""  # Detection branch selection mode: Branch selection mode for an explicit detection route fork. | Allowed values (canonical IDs): fixed, exclusive, multiple
          items:  # Detection branch items: Explicit route-owned branch definitions nested under a detection sequence item.
            -
              branch_id: ""  # Detection branch ID: Stable branch identifier scoped to the branch block.
              label: ""  # Detection branch label: Human-readable label for a detection branch.
              sequence:  # Detection branch sequence: Linear branch-local traversal sequence for a detection route fork; each branch-local item must declare exactly one of optical_path_element_id or endpoint_id.
                -
                  optical_path_element_id: ""  # Detection branch optical path element reference: Stable optical-path-element reference for the optical_path_element_id variant of a detection branch-local tagged union item.
                  endpoint_id: ""  # Detection branch endpoint reference: Stable endpoint reference for the endpoint_id variant of a detection branch-local tagged union item.

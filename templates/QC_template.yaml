# AUTO-GENERATED FILE. Do not edit manually.
# Source schema: schema/QC_policy.yaml

# -----------------------------------------------------------------------------
# SCHEMA VERSION
# -----------------------------------------------------------------------------
# Schema Version: Locks interpretation of the QC ledger shape.
# Allowed values: 1
schema_version: 1

# -----------------------------------------------------------------------------
# RECORD TYPE
# -----------------------------------------------------------------------------
# Record Type: Distinguishes QC ledgers from maintenance ledgers.
# Allowed values: qc_session
record_type: "qc_session"

# -----------------------------------------------------------------------------
# QC ID
# -----------------------------------------------------------------------------
# Qc Id: Stable unique key for the QC session.
qc_id: ""

# -----------------------------------------------------------------------------
# MICROSCOPE
# -----------------------------------------------------------------------------
# Microscope: Links QC records to the instrument registry.
microscope: ""

# -----------------------------------------------------------------------------
# STARTED UTC
# -----------------------------------------------------------------------------
# Started Utc: Required for chronology and longitudinal analysis.
started_utc: ""

# -----------------------------------------------------------------------------
# ENDED UTC
# -----------------------------------------------------------------------------
# Ended Utc: Useful for duration and traceability, but not essential.
ended_utc: ""

# -----------------------------------------------------------------------------
# PERFORMED BY
# -----------------------------------------------------------------------------
# Performed By: Traceability for who executed the QC session.
performed_by: ""

# -----------------------------------------------------------------------------
# REASON
# -----------------------------------------------------------------------------
# Reason: Explains why the QC was run (monthly_qc, post_service, troubleshooting, etc.).
reason: ""

# -----------------------------------------------------------------------------
# SUMMARY
# -----------------------------------------------------------------------------
# Summary: Human-readable session summary without verdict semantics.
summary: ""

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# Notes: Holds contextual nuance that does not fit structured fields.
notes: ""

# -----------------------------------------------------------------------------
# PERFORMED
# -----------------------------------------------------------------------------
# Performed: Defines what QC activities were actually performed.
performed:
  -
    # Qc Type: Main structured QC activity classifier.
    # Allowed values (canonical IDs): laser_power, psf, alignment, stage, other
    qc_type: ""
    # Details: Captures protocol-level context for each performed activity.
    details: ""
    # Artifacts: Links performed activities to artifact pointers when available.
    artifacts:
      - ""

# -----------------------------------------------------------------------------
# INPUTS HUMAN
# -----------------------------------------------------------------------------
# Inputs Human: Flexible slot for manual checks and environment readings.
inputs_human:
  -
    # Metric Id: Free-text metric identifier that should include specific context (for example objective and wavelength) for longitudinal plotting.
    metric_id: ""
    # Metric Class: Controlled category used to group related metrics across instruments and modalities.
    # Allowed values (canonical IDs): fwhm_lateral, fwhm_axial, signal_to_noise, chromatic_shift, stage_repeatability, laser_power, illumination_uniformity, detector_dark_noise
    metric_class: ""
    # Value: Stores numeric or categorical observations.
    value: ""
    # Unit: Unit may be empty for checklist values.
    unit: ""
    # Details: Useful supporting context for manual metrics.
    details: ""

# -----------------------------------------------------------------------------
# LASER INPUTS HUMAN
# -----------------------------------------------------------------------------
# Laser Inputs Human: Laser QC should be represented with structured measurement metadata.
laser_inputs_human:
  # Measurement Position: Measurement position determines comparability across sessions.
  # Allowed values (canonical IDs): at_objective, pre_objective, fiber_output
  measurement_position: ""
  # Measurement Position Details: Documents adapters/objective/sensor placement for reproducibility.
  measurement_position_details: ""
  power_meter:
    # Model: Identifies the external measurement device, if applicable.
    model: ""
    # Serial: Useful traceability when available.
    serial: ""
    # Calibration Due: Good practice for calibrated instruments, but not always available.
    calibration_due: ""
    # Integration Time S: Helpful for noisy traces and protocol comparability.
    integration_time_s: 0.0
    # Notes: Captures attenuators, adapters, sensor type, or exceptional setup details.
    notes: ""
  # Linearity Series: At least one structured laser data series should exist for laser QC.
  linearity_series:
    -
      # Laser: Identifies the laser line under test.
      laser: ""
      # Setpoint Units: Required to interpret linearity points correctly.
      # Allowed values (canonical IDs): percent, mW_setpoint
      setpoint_units: ""
      # Power Units: Required to interpret the measured power values.
      power_units: ""
      # Points: Inline points are required unless the full series is stored as an artifact.
      points:
        -
          # Setpoint: Required for linearity analysis.
          setpoint: 0.0
          # Power: Required for linearity analysis.
          power: 0.0
      # Csv Artifact: Preferred for dense or replicated series.
      csv_artifact: ""
      # Details: Captures protocol specifics for each sweep.
      details: ""
  # Stability Series: Stability-only laser QC should still be representable as structured series.
  stability_series:
    -
      # Laser: Identifies the laser line under test.
      laser: ""
      # Setpoint: Needed to interpret drift measurements.
      setpoint: 0.0
      # Setpoint Units: Keeps setpoint semantics machine-readable.
      # Allowed values (canonical IDs): percent, mW_setpoint
      setpoint_units: ""
      # Sampling: Required when full timestamps are not embedded inline.
      sampling: ""
      # Power Units: Required to interpret power values.
      power_units: ""
      # Timepoints: Inline timepoints are required unless a CSV artifact is provided.
      timepoints:
        -
          # T S: Required for time-resolved stability analysis.
          t_s: 0.0
          # Power: Required for stability analysis.
          power: 0.0
      # Csv Artifact: Preferred for long time series.
      csv_artifact: ""
      # Details: Captures context for the trace.
      details: ""
  # Single Point Measurements: Supports quick single-point laser checks without creating full series arrays.
  single_point_measurements:
    -
      # Laser: Identifies the laser line under test.
      laser: ""
      # Setpoint: Required to interpret the measurement target.
      setpoint: 0.0
      # Setpoint Units: Keeps setpoint semantics machine-readable.
      # Allowed values (canonical IDs): percent, mW_setpoint
      setpoint_units: ""
      # Power: Captures measured output power for quick checks.
      power: 0.0
      # Power Units: Required to interpret the measured power values.
      power_units: ""
      # Details: Optional context such as tolerance checks or pass/fail notes.
      details: ""

# -----------------------------------------------------------------------------
# COMPUTED PROVENANCE
# -----------------------------------------------------------------------------
# Computed Provenance: Computed metrics should always carry provenance.
computed_provenance:
  # Pipeline: Identifies the analysis pipeline.
  pipeline: ""
  # Version: Pipeline version is essential for reproducibility.
  version: ""
  # Git Commit: Highly useful when the analysis code is version-controlled.
  git_commit: ""
  # Run Utc: Needed to know when analysis was produced.
  run_utc: ""

# -----------------------------------------------------------------------------
# METRICS COMPUTED
# -----------------------------------------------------------------------------
# Metrics Computed: Computed outputs are optional at authoring time but central after analysis.
metrics_computed:
  -
    # Metric Id: Free-text metric identifier that should include specific context (for example objective and wavelength) for longitudinal plotting.
    metric_id: ""
    # Metric Class: Controlled category used to group related computed metrics for filtering and automation.
    # Allowed values (canonical IDs): fwhm_lateral, fwhm_axial, signal_to_noise, chromatic_shift, stage_repeatability, laser_power, illumination_uniformity, detector_dark_noise
    metric_class: ""
    # Value: Numeric or categorical result payload.
    value: ""
    # Unit: Required for display and threshold interpretation.
    unit: ""
    # Details: Stores fit details or aggregation notes.
    details: ""

# -----------------------------------------------------------------------------
# EVALUATION
# -----------------------------------------------------------------------------
# Evaluation: Evaluation is expected after benchmark comparison, not at initial authoring.
evaluation:
  # Benchmark Ref: Makes pass/warn/fail judgments reproducible.
  benchmark_ref: ""
  # Evaluated Utc: Records when the benchmark comparison was run.
  evaluated_utc: ""
  # Evaluated By: Identifies the CI actor or pipeline.
  evaluated_by: ""
  # Overall Status: Session-level QC verdict.
  # Allowed values (canonical IDs): pass, warn, fail
  overall_status: ""
  # Results: Machine-readable per-metric evaluation breakdown.
  results:
    -
      # Metric Id: Links the verdict to a metric.
      metric_id: ""
      # Status: Per-metric verdict.
      # Allowed values (canonical IDs): pass, warn, fail
      status: ""
      # Threshold: Human-readable decision rule recorded with the verdict.
      threshold: ""
      # Message: Optional explanatory note.
      message: ""
  # Computed Provenance: Captures provenance metadata for automated evaluation computations.
  computed_provenance:
    # Software Version: Records the AIC-Chronicle software version used to compute results.
    software_version: ""
    # Algorithm Id: Identifies the exact algorithm/function used for generated evaluation results.
    algorithm_id: ""

# -----------------------------------------------------------------------------
# ARTIFACTS
# -----------------------------------------------------------------------------
# Artifacts: Ledger pointers to raw data, tables, previews, and reports.
artifacts:
  -
    # Artifact Id: Stable pointer key referenced elsewhere in the QC ledger.
    artifact_id: ""
    # Role: Artifact role drives UI treatment.
    # Allowed values (canonical IDs): table, raw_image, preview, report, other
    role: ""
    # Uri: Must resolve as either an OMERO URI (for example, omero://image/12345) or a local repository-relative path (for example, local://qc/sessions/...).
    uri: ""
    # Description: Human-readable description of the artifact.
    description: ""
    # Checksum Sha256: Useful integrity check for non-OMERO artifacts.
    checksum_sha256: ""

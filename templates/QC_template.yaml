# AUTO-GENERATED FILE. Do not edit manually.
# Source schema: schema/QC_policy.yaml

# -----------------------------------------------------------------------------
# SCHEMA VERSION
# -----------------------------------------------------------------------------
schema_version: 1  # Schema Version: Locks interpretation of the QC ledger shape. | Allowed values: 1

# -----------------------------------------------------------------------------
# RECORD TYPE
# -----------------------------------------------------------------------------
record_type: "qc_session"  # Record Type: Distinguishes QC ledgers from maintenance ledgers. | Allowed values: qc_session

# -----------------------------------------------------------------------------
# QC ID
# -----------------------------------------------------------------------------
qc_id: ""  # Qc Id: Stable unique key for the QC session.

# -----------------------------------------------------------------------------
# MICROSCOPE
# -----------------------------------------------------------------------------
microscope: ""  # Microscope: Links QC records to the instrument registry.

# -----------------------------------------------------------------------------
# STARTED UTC
# -----------------------------------------------------------------------------
started_utc: ""  # Started Utc: Required for chronology and longitudinal analysis.

# -----------------------------------------------------------------------------
# ENDED UTC
# -----------------------------------------------------------------------------
ended_utc: ""  # Ended Utc: Useful for duration and traceability, but not essential.

# -----------------------------------------------------------------------------
# PERFORMED BY
# -----------------------------------------------------------------------------
performed_by: ""  # Performed By: Traceability for who executed the QC session.

# -----------------------------------------------------------------------------
# REASON
# -----------------------------------------------------------------------------
reason: ""  # Reason: Explains why the QC was run (monthly_qc, post_service, troubleshooting, etc.).

# -----------------------------------------------------------------------------
# SUMMARY
# -----------------------------------------------------------------------------
summary: ""  # Summary: Human-readable session summary without verdict semantics.

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
notes: ""  # Notes: Holds contextual nuance that does not fit structured fields.

# -----------------------------------------------------------------------------
# PERFORMED
# -----------------------------------------------------------------------------
performed:  # Performed: Defines what QC activities were actually performed.
  -
    qc_type: ""  # Qc Type: Main structured QC activity classifier. | Allowed values (canonical IDs): laser_power, psf, alignment, stage, other
    details: ""  # Details: Captures protocol-level context for each performed activity.
    artifacts:  # Artifacts: Links performed activities to artifact pointers when available.
      - ""

# -----------------------------------------------------------------------------
# INPUTS HUMAN
# -----------------------------------------------------------------------------
inputs_human:  # Inputs Human: Flexible slot for manual checks and environment readings.
  -
    metric_id: ""  # Metric Id: Free-text metric identifier that should include specific context (for example objective and wavelength) for longitudinal plotting.
    metric_class: ""  # Metric Class: Controlled category used to group related metrics across instruments and modalities. | Allowed values (canonical IDs): fwhm_lateral, fwhm_axial, signal_to_noise, chromatic_shift, stage_repeatability, laser_power, illumination_uniformity, detector_dark_noise
    value: ""  # Value: Stores numeric or categorical observations.
    unit: ""  # Unit: Unit may be empty for checklist values.
    details: ""  # Details: Useful supporting context for manual metrics.

# -----------------------------------------------------------------------------
# LASER INPUTS HUMAN
# -----------------------------------------------------------------------------
laser_inputs_human:  # Laser Inputs Human: Laser QC should be represented with structured measurement metadata.
  measurement_position: ""  # Measurement Position: Measurement position determines comparability across sessions. | Allowed values (canonical IDs): at_objective, pre_objective, fiber_output
  measurement_position_details: ""  # Measurement Position Details: Documents adapters/objective/sensor placement for reproducibility.
  power_meter:
    model: ""  # Model: Identifies the external measurement device, if applicable.
    serial: ""  # Serial: Useful traceability when available.
    calibration_due: ""  # Calibration Due: Good practice for calibrated instruments, but not always available.
    integration_time_s: 0.0  # Integration Time S: Helpful for noisy traces and protocol comparability.
    notes: ""  # Notes: Captures attenuators, adapters, sensor type, or exceptional setup details.
  linearity_series:  # Linearity Series: At least one structured laser data series should exist for laser QC.
    -
      laser: ""  # Laser: Identifies the laser line under test.
      setpoint_units: ""  # Setpoint Units: Required to interpret linearity points correctly. | Allowed values (canonical IDs): percent, mW_setpoint
      power_units: ""  # Power Units: Required to interpret the measured power values.
      points:  # Points: Inline points are required unless the full series is stored as an artifact.
        -
          setpoint: 0.0  # Setpoint: Required for linearity analysis.
          power: 0.0  # Power: Required for linearity analysis.
      csv_artifact: ""  # Csv Artifact: Preferred for dense or replicated series.
      details: ""  # Details: Captures protocol specifics for each sweep.
  stability_series:  # Stability Series: Stability-only laser QC should still be representable as structured series.
    -
      laser: ""  # Laser: Identifies the laser line under test.
      setpoint: 0.0  # Setpoint: Needed to interpret drift measurements.
      setpoint_units: ""  # Setpoint Units: Keeps setpoint semantics machine-readable. | Allowed values (canonical IDs): percent, mW_setpoint
      sampling: ""  # Sampling: Required when full timestamps are not embedded inline.
      power_units: ""  # Power Units: Required to interpret power values.
      timepoints:  # Timepoints: Inline timepoints are required unless a CSV artifact is provided.
        -
          t_s: 0.0  # T S: Required for time-resolved stability analysis.
          power: 0.0  # Power: Required for stability analysis.
      csv_artifact: ""  # Csv Artifact: Preferred for long time series.
      details: ""  # Details: Captures context for the trace.
  single_point_measurements:  # Single Point Measurements: Supports quick single-point laser checks without creating full series arrays.
    -
      laser: ""  # Laser: Identifies the laser line under test.
      setpoint: 0.0  # Setpoint: Required to interpret the measurement target.
      setpoint_units: ""  # Setpoint Units: Keeps setpoint semantics machine-readable. | Allowed values (canonical IDs): percent, mW_setpoint
      power: 0.0  # Power: Captures measured output power for quick checks.
      power_units: ""  # Power Units: Required to interpret the measured power values.
      details: ""  # Details: Optional context such as tolerance checks or pass/fail notes.

# -----------------------------------------------------------------------------
# COMPUTED PROVENANCE
# -----------------------------------------------------------------------------
computed_provenance:  # Computed Provenance: Computed metrics should always carry provenance.
  pipeline: ""  # Pipeline: Identifies the analysis pipeline.
  version: ""  # Version: Pipeline version is essential for reproducibility.
  git_commit: ""  # Git Commit: Highly useful when the analysis code is version-controlled.
  run_utc: ""  # Run Utc: Needed to know when analysis was produced.

# -----------------------------------------------------------------------------
# METRICS COMPUTED
# -----------------------------------------------------------------------------
metrics_computed:  # Metrics Computed: Computed outputs are optional at authoring time but central after analysis.
  -
    metric_id: ""  # Metric Id: Free-text metric identifier that should include specific context (for example objective and wavelength) for longitudinal plotting.
    metric_class: ""  # Metric Class: Controlled category used to group related computed metrics for filtering and automation. | Allowed values (canonical IDs): fwhm_lateral, fwhm_axial, signal_to_noise, chromatic_shift, stage_repeatability, laser_power, illumination_uniformity, detector_dark_noise
    value: ""  # Value: Numeric or categorical result payload.
    unit: ""  # Unit: Required for display and threshold interpretation.
    details: ""  # Details: Stores fit details or aggregation notes.

# -----------------------------------------------------------------------------
# EVALUATION
# -----------------------------------------------------------------------------
evaluation:  # Evaluation: Evaluation is expected after benchmark comparison, not at initial authoring.
  benchmark_ref: ""  # Benchmark Ref: Makes pass/warn/fail judgments reproducible.
  evaluated_utc: ""  # Evaluated Utc: Records when the benchmark comparison was run.
  evaluated_by: ""  # Evaluated By: Identifies the CI actor or pipeline.
  overall_status: ""  # Overall Status: Session-level QC verdict. | Allowed values (canonical IDs): pass, warn, fail
  results:  # Results: Machine-readable per-metric evaluation breakdown.
    -
      metric_id: ""  # Metric Id: Links the verdict to a metric.
      status: ""  # Status: Per-metric verdict. | Allowed values (canonical IDs): pass, warn, fail
      threshold: ""  # Threshold: Human-readable decision rule recorded with the verdict.
      message: ""  # Message: Optional explanatory note.
  computed_provenance:  # Computed Provenance: Captures provenance metadata for automated evaluation computations.
    software_version: ""  # Software Version: Records the AIC-Chronicle software version used to compute results.
    algorithm_id: ""  # Algorithm Id: Identifies the exact algorithm/function used for generated evaluation results.

# -----------------------------------------------------------------------------
# ARTIFACTS
# -----------------------------------------------------------------------------
artifacts:  # Artifacts: Ledger pointers to raw data, tables, previews, and reports.
  -
    artifact_id: ""  # Artifact Id: Stable pointer key referenced elsewhere in the QC ledger.
    role: ""  # Role: Artifact role drives UI treatment. | Allowed values (canonical IDs): table, raw_image, preview, report, other
    uri: ""  # Uri: Must resolve as either an OMERO URI (for example, omero://image/12345) or a local repository-relative path (for example, local://qc/sessions/...).
    description: ""  # Description: Human-readable description of the artifact.
    checksum_sha256: ""  # Checksum Sha256: Useful integrity check for non-OMERO artifacts.
